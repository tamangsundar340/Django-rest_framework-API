# Basic django rest_framework API

# Basic setup
create virtual enviroment
activate virtual enviroment
install django
install requirements
run django

# Super user credential
username = admin
password = pass
